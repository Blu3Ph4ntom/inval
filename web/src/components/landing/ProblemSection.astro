<section class="sect" id="problem">
  <div class="wrap">

    <p class="sect-eyebrow" data-reveal>The Problem</p>
    <h2 class="sect-heading" data-reveal>Every change triggers<br />a full recompute.</h2>
    <p class="sect-body" data-reveal>
      Sidebar resizes, dynamic-height rows, responsive breakpoints — applications have no
      abstraction for <em>what geometry changed, and what depends on it</em>.
    </p>

    <div class="symptoms" data-stagger>
      <div class="symptom">
        <p class="symptom-name">Dashboard lag</p>
        <p class="symptom-desc">Resize one panel. Every widget repaints. No dependency tracking means no optimisation.</p>
      </div>
      <div class="symptom">
        <p class="symptom-name">Scroll jitter</p>
        <p class="symptom-desc">Dynamic-height rows don't know their own dependencies. Total height recalculates on every scroll event.</p>
      </div>
      <div class="symptom">
        <p class="symptom-name">Stale layouts</p>
        <p class="symptom-desc">Manual memoization misses dependencies. A breakpoint change silently renders wrong data.</p>
      </div>
      <div class="symptom">
        <p class="symptom-name">Invisible root cause</p>
        <p class="symptom-desc">No tool surfaces which geometry invalidated and why. Debugging is trial and error.</p>
      </div>
    </div>

    <div class="issues" data-reveal>
      <p class="issues-label">Evidence — open issues in production libraries</p>
      <div class="issues-list">
        <a href="https://github.com/TanStack/virtual/issues/832" target="_blank" rel="noopener" class="issue-row">
          <span class="issue-ref">TanStack Virtual · #832</span>
          <span class="issue-text">Scrolling with dynamic height lags and stutters</span>
        </a>
        <a href="https://github.com/TanStack/virtual/issues/659" target="_blank" rel="noopener" class="issue-row">
          <span class="issue-ref">TanStack Virtual · #659</span>
          <span class="issue-text">Scrolling up with dynamic heights stutters</span>
        </a>
        <a href="https://github.com/bvaughn/react-window/issues/741" target="_blank" rel="noopener" class="issue-row">
          <span class="issue-ref">react-window · #741</span>
          <span class="issue-text">VariableSizeList causes major scroll jitters</span>
        </a>
        <span class="issue-text">and many more...</span>
      </div>
    </div>

  </div>
</section>

<style>
  .sect {
    padding: var(--sp-24) 0;
    border-top: 1px solid var(--border);
  }

  .sect-eyebrow {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--fg-muted);
    text-transform: uppercase;
    letter-spacing: 0.1em;
    margin-bottom: var(--sp-4);
  }

  .sect-heading {
    font-size: clamp(var(--text-2xl), 4vw, var(--text-3xl));
    font-weight: 600;
    line-height: 1.15;
    letter-spacing: -0.03em;
    margin-bottom: var(--sp-5);
  }

  .sect-body {
    font-size: var(--text-lg);
    color: var(--fg-secondary);
    max-width: 600px;
    line-height: 1.65;
    margin-bottom: var(--sp-12);
  }

  .sect-body em {
    font-style: normal;
    color: var(--fg);
  }

  /* 2x2 symptom grid */
  .symptoms {
    display: grid;
    grid-template-columns: 1fr 1fr;
    margin-bottom: var(--sp-12);
  }

  .symptom {
    padding: var(--sp-6);
    border-top: 1px solid var(--border);
    border-right: 1px solid var(--border);
  }

  .symptom:nth-child(2n) { border-right: none; }
  .symptom:nth-child(3),
  .symptom:nth-child(4) { border-top: 1px solid var(--border); }

  .symptom-name {
    font-size: var(--text-sm);
    font-weight: 500;
    margin-bottom: var(--sp-2);
  }

  .symptom-desc {
    font-size: var(--text-sm);
    color: var(--fg-secondary);
    line-height: 1.55;
  }

  /* Issues table */
  .issues-label {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--fg-muted);
    text-transform: uppercase;
    letter-spacing: 0.08em;
    margin-bottom: var(--sp-3);
  }

  .issues-list {
    border-top: 1px solid var(--border);
  }

  .issue-row {
    display: flex;
    align-items: center;
    gap: var(--sp-6);
    padding: var(--sp-4) 0;
    border-bottom: 1px solid var(--border);
    transition: opacity var(--duration) var(--ease);
  }

  .issue-row:hover { opacity: 0.7; }

  .issue-ref {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--fg);
    min-width: 200px;
    white-space: nowrap;
  }

  .issue-text {
    font-size: var(--text-sm);
    color: var(--fg-muted);
  }

  @media (max-width: 640px) {
    .symptoms { grid-template-columns: 1fr; }
    .symptom { border-right: none; }
    .issue-row { flex-direction: column; align-items: flex-start; gap: var(--sp-1); }
    .issue-ref { min-width: auto; }
  }
</style>
