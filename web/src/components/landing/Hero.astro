<section class="hero">
  <div class="wrap hero-inner">
    <p class="hero-tag" data-reveal>
      <span class="hero-dot"></span>
      <code>npm i @blu3ph4ntom/inval</code>
    </p>

    <h1 class="hero-h" data-reveal>
      Layout invalidation,<br />solved.
    </h1>

    <p class="hero-sub" data-reveal>
      A dependency graph engine for incremental layout computation.<br />
      Not a framework. Not a renderer. The missing primitive.
    </p>

    <div class="hero-actions" data-reveal>
      <a href="/docs/getting-started" class="btn-solid">Get Started</a>
      <a href="https://github.com/Blu3Ph4ntom/inval" target="_blank" rel="noopener" class="btn-ghost">GitHub</a>
    </div>

    <div class="hero-bench" data-reveal>
      <div class="bench-row">
        <span class="bench-label">naive</span>
        <div class="bench-track"><div class="bench-fill bench-slow"></div></div>
        <span class="bench-num">417K ops/s</span>
      </div>
      <div class="bench-row">
        <span class="bench-label">inval</span>
        <div class="bench-track"><div class="bench-fill bench-fast"></div></div>
        <span class="bench-num is-highlight">4,230K ops/s</span>
      </div>
    </div>
  </div>
</section>

<style>
  .hero {
    padding: calc(var(--nav-h) + var(--sp-20)) 0 var(--sp-20);
    text-align: center;
  }

  .hero-inner {
    display: flex;
    flex-direction: column;
    align-items: center;
  }

  .hero-tag {
    display: inline-flex;
    align-items: center;
    gap: var(--sp-2);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--fg-secondary);
    border: 1px solid var(--border);
    border-radius: var(--radius-full);
    padding: var(--sp-1) var(--sp-3);
    margin-bottom: var(--sp-8);
  }

  .hero-tag code {
    font-size: inherit;
  }

  .hero-dot {
    width: 5px;
    height: 5px;
    border-radius: 50%;
    background: var(--green);
  }

  .hero-h {
    font-size: clamp(var(--text-4xl), 8vw, var(--text-5xl));
    font-weight: 600;
    line-height: 1.05;
    letter-spacing: -0.04em;
    color: var(--fg);
    margin-bottom: var(--sp-6);
    max-width: 820px;
  }

  .hero-sub {
    font-size: var(--text-lg);
    line-height: 1.6;
    color: var(--fg-secondary);
    max-width: 520px;
    margin-bottom: var(--sp-10);
  }

  .hero-actions {
    display: flex;
    gap: var(--sp-3);
    margin-bottom: var(--sp-16);
  }

  .btn-solid {
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--accent-fg);
    background: var(--accent);
    padding: var(--sp-2) var(--sp-5);
    border-radius: var(--radius-sm);
    transition: opacity var(--duration) var(--ease);
  }

  .btn-solid:hover { opacity: 0.85; }

  .btn-ghost {
    font-size: var(--text-sm);
    font-weight: 500;
    color: var(--fg-secondary);
    padding: var(--sp-2) var(--sp-5);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    transition: color var(--duration) var(--ease), border-color var(--duration) var(--ease);
  }

  .btn-ghost:hover {
    color: var(--fg);
    border-color: var(--border-hover);
  }

  /* Benchmark bars */
  .hero-bench {
    width: 100%;
    max-width: 420px;
    display: flex;
    flex-direction: column;
    gap: var(--sp-3);
  }

  .bench-row {
    display: flex;
    align-items: center;
    gap: var(--sp-3);
  }

  .bench-label {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--fg-muted);
    width: 40px;
    text-align: right;
  }

  .bench-track {
    flex: 1;
    height: 3px;
    background: var(--border);
    border-radius: 2px;
    overflow: hidden;
  }

  .bench-fill {
    height: 100%;
    border-radius: 2px;
    animation: grow 1.2s var(--ease) forwards;
  }

  .bench-slow {
    width: 10%;
    background: var(--fg-muted);
  }

  .bench-fast {
    width: 100%;
    background: var(--fg);
  }

  .bench-num {
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--fg-muted);
    width: 100px;
    text-align: left;
  }

  .bench-num.is-highlight { color: var(--fg); }

  @keyframes grow {
    from { width: 0; }
  }

  @media (max-width: 640px) {
    .hero { padding: calc(var(--nav-h) + var(--sp-12)) 0 var(--sp-12); }
    .hero-sub br { display: none; }
  }
</style>
