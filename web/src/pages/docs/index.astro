---
import DocsLayout from '../../layouts/DocsLayout.astro'
import DocsSidebar from '../../components/docs/DocsSidebar.astro'
import DocsToc from '../../components/docs/DocsToc.astro'
import CodeBlock from '../../components/docs/CodeBlock.astro'
import Callout from '../../components/docs/Callout.astro'
import { withBase } from '../../utils/base'

const currentPage = '/docs/'

const headings = [
  { id: 'what-is-inval', text: 'What is Inval?', level: 2 },
  { id: 'why-inval', text: 'Why Inval?', level: 2 },
  { id: 'how-it-works', text: 'How It Works', level: 2 },
  { id: 'principles', text: 'Principles', level: 2 },
]
---

<DocsLayout title="Introduction — Inval Docs" description="Learn what Inval is and why you need it.">
  <DocsSidebar slot="sidebar" currentPage={currentPage} />
  <DocsToc slot="toc" headings={headings} />

  <div class="doc-page">
    <nav class="breadcrumb">
      <a href={withBase('/docs/')}>Docs</a>
      <span class="bc-sep">/</span>
      <span>Introduction</span>
    </nav>

    <h1 class="doc-h1">Introduction</h1>
    <p class="doc-lead">Deterministic, incremental layout invalidation engine.</p>

    <section id="what-is-inval">
      <h2 class="doc-h2">What is Inval?</h2>
      <p class="doc-p">Inval is a <strong>dependency graph engine</strong> for tracking computed values. It's not a framework, not a renderer, and not a state management library.</p>
      <p class="doc-p">It answers one question: <em>"What geometry changed, and what depends on it?"</em></p>
      <p class="doc-p">When you build interactive UIs — dashboards, virtualized lists, resizable panels — layout changes cascade. A sidebar width change affects content width, which changes text wrapping, which shifts card heights, which breaks scroll positions.</p>
      <p class="doc-p">Most apps handle this by recomputing everything on every change. Inval gives you the primitive to do it <strong>incrementally</strong>.</p>
    </section>

    <section id="why-inval">
      <h2 class="doc-h2">Why Inval?</h2>
      <p class="doc-p">Popular virtualization libraries struggle with dynamic height issues:</p>
      <ul class="doc-ul">
        <li><strong>TanStack Virtual</strong> — issues #832, #659: scrolling with dynamic height lags and stutters</li>
        <li><strong>react-window</strong> — issue #741: VariableSizeList causes major scroll jitters</li>
      </ul>
      <p class="doc-p"><strong>Root cause:</strong> No dependency graph means recomputing ALL nodes on ANY change.</p>

      <Callout type="tip">
        <p>Inval solves this at the engine level. Declare what depends on what, and the engine handles incremental updates automatically.</p>
      </Callout>
    </section>

    <section id="quick-example">
      <h2 class="doc-h2">Quick Example</h2>

      <CodeBlock
        filename="example.ts"
        lang="typescript"
        code={`import { input, node, why } from '@blu3ph4ntom/inval'

// Declare inputs
const width = input(800)
const height = input(600)

// Computed node with dependencies
const area = node({
  dependsOn: { w: width, h: height },
  compute: ({ w, h }) => w * h
})

area.get()        // 480000 — computes lazily
area.get()        // 480000 — cached, zero cost

width.set(1000)
area.get()        // 600000 — recomputes only what changed

why(area)         // ['area', 'width'] — trace invalidation path`}
      />
    </section>

    <section id="key-principles">
      <h2 class="doc-h2">Key Principles</h2>
      <div class="principles-grid">
        <div class="principle-card">
          <h3 class="doc-h3">DAG</h3>
          <p class="doc-p">Nodes form a directed acyclic graph. Cycles are detected at construction time.</p>
        </div>
        <div class="principle-card">
          <h3 class="doc-h3">Lazy</h3>
          <p class="doc-p"><code class="doc-code">node.get()</code> recomputes only if dirty. Returns cached value otherwise.</p>
        </div>
        <div class="principle-card">
          <h3 class="doc-h3">Incremental</h3>
          <p class="doc-p"><code class="doc-code">input.set()</code> walks children, marks only affected nodes dirty.</p>
        </div>
        <div class="principle-card">
          <h3 class="doc-h3">Pure</h3>
          <p class="doc-p">No DOM. You measure, you pass values in. Framework agnostic.</p>
        </div>
        <div class="principle-card">
          <h3 class="doc-h3">Zero deps</h3>
          <p class="doc-p">Pure TypeScript. 12KB packed. No external runtime dependencies.</p>
        </div>
        <div class="principle-card">
          <h3 class="doc-h3">Debuggable</h3>
          <p class="doc-p"><code class="doc-code">why()</code>, <code class="doc-code">inspect()</code>, <code class="doc-code">toDot()</code> for full visibility.</p>
        </div>
      </div>
    </section>

    <section id="next-steps">
      <h2 class="doc-h2">Next Steps</h2>
      <div class="next-steps">
        <a href={withBase('/docs/getting-started')} class="next-step-card">
          <span class="next-step-label">Next</span>
          <h3 class="doc-h3">Getting Started →</h3>
          <p class="doc-p">Install Inval and build your first dependency graph in 5 minutes.</p>
        </a>
      </div>
    </section>

    <div class="page-nav">
      <div></div>
        <a href={withBase('/docs/getting-started')}>
        <span>Getting Started</span>
        <span>→</span>
      </a>
    </div>
  </div>
</DocsLayout>

<style>
  .doc-page {
    max-width: 680px;
    padding: var(--sp-12) var(--sp-10);
  }

  .breadcrumb {
    display: flex;
    align-items: center;
    gap: var(--sp-2);
    font-family: var(--font-mono);
    font-size: var(--text-xs);
    color: var(--fg-muted);
    margin-bottom: var(--sp-8);
  }

  .breadcrumb a { color: var(--fg-muted); transition: color var(--duration) var(--ease); }
  .breadcrumb a:hover { color: var(--fg-secondary); }
  .bc-sep { color: var(--fg-muted); opacity: 0.4; }

  .doc-h1 {
    font-size: clamp(1.75rem, 4vw, 2.25rem);
    font-weight: 600;
    line-height: 1.15;
    letter-spacing: -0.03em;
    margin-bottom: var(--sp-3);
  }

  .doc-lead {
    font-size: var(--text-lg);
    color: var(--fg-secondary);
    margin-bottom: var(--sp-10);
    line-height: 1.65;
  }

  .doc-h2 {
    font-size: var(--text-xl);
    font-weight: 600;
    letter-spacing: -0.02em;
    margin-top: var(--sp-12);
    margin-bottom: var(--sp-4);
    padding-top: var(--sp-12);
    border-top: 1px solid var(--border);
  }

  .doc-h2:first-of-type { margin-top: var(--sp-8); padding-top: 0; border-top: none; }

  .doc-h3 {
    font-size: var(--text-base);
    font-weight: 600;
    margin-top: var(--sp-8);
    margin-bottom: var(--sp-3);
  }

  .doc-p {
    font-size: var(--text-base);
    color: var(--fg-secondary);
    line-height: 1.7;
    margin-bottom: var(--sp-4);
  }

  .doc-ul {
    list-style: none;
    margin-bottom: var(--sp-4);
    display: flex;
    flex-direction: column;
    gap: var(--sp-2);
  }

  .doc-ul li {
    font-size: var(--text-base);
    color: var(--fg-secondary);
    line-height: 1.6;
    padding-left: var(--sp-4);
    position: relative;
  }

  .doc-ul li::before {
    content: '—';
    position: absolute;
    left: 0;
    color: var(--fg-muted);
    font-size: var(--text-xs);
  }

  .doc-code {
    font-family: var(--font-mono);
    font-size: 0.86em;
    color: var(--fg);
    background: var(--bg-muted);
    padding: 3px 8px;
    border-radius: 6px;
    border: 1px solid var(--border);
    font-weight: 450;
    letter-spacing: 0.01em;
    transition: all var(--duration) var(--ease);
  }

  .doc-code:hover {
    background: var(--bg-elevated);
    border-color: var(--border-hover);
  }

  strong { color: var(--fg); font-weight: 500; }

  .principles-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: var(--sp-4);
    margin: var(--sp-6) 0;
  }

  .principle-card {
    background: var(--bg-subtle);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: var(--sp-5);
    transition: border-color var(--duration) var(--ease);
  }

  .principle-card:hover {
    border-color: var(--fg-muted);
  }

  .principle-card .doc-h3 {
    font-family: var(--font-mono);
    margin-top: 0;
  }

  .principle-card .doc-p {
    font-size: var(--text-sm);
    margin-bottom: 0;
  }

  .next-steps {
    margin: var(--sp-8) 0;
  }

  .next-step-card {
    display: block;
    background: var(--bg-subtle);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: var(--sp-6);
    transition: border-color var(--duration) var(--ease);
  }

  .next-step-card:hover {
    border-color: var(--fg-muted);
    text-decoration: none;
  }

  .next-step-label {
    font-size: var(--text-xs);
    color: var(--fg-muted);
    text-transform: uppercase;
    letter-spacing: 0.05em;
  }

  .next-step-card .doc-h3 {
    color: var(--fg);
    margin-top: var(--sp-1);
    margin-bottom: var(--sp-2);
  }

  .next-step-card .doc-p {
    font-size: var(--text-sm);
    margin-bottom: 0;
  }

  /* Page nav */
  .page-nav {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-top: var(--sp-16);
    padding-top: var(--sp-6);
    border-top: 1px solid var(--border);
  }

  .page-nav a {
    display: flex;
    align-items: center;
    gap: var(--sp-2);
    font-size: var(--text-sm);
    color: var(--fg-secondary);
    border: 1px solid var(--border);
    border-radius: var(--radius-sm);
    padding: var(--sp-2) var(--sp-4);
    transition: color var(--duration) var(--ease), border-color var(--duration) var(--ease);
  }

  .page-nav a:hover { color: var(--fg); border-color: var(--border-hover); }

  @media (max-width: 960px) {
    .doc-page { padding: var(--sp-8) var(--sp-5); }
  }

  @media (max-width: 640px) {
    .principles-grid { grid-template-columns: 1fr; }
  }
</style>
